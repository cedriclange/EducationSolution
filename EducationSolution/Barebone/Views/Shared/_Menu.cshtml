@model Barebone.ViewModels.Shared.MenuViewModel
<ul class="nav flex-column">
    @foreach (var menuItem in Model.MenuItems)
    {
        await Html.RenderPartialAsync("_MenuItem", menuItem);
    }
</ul>